import React from 'react';
import ReactDOM from 'react-dom'; // Integra o react com a DOM do navegador
import App from './App';


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
